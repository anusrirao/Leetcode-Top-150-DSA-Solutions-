/**
 * LeetCode: Merge Sorted Array (Easy)
 * Link: https:https://leetcode.com/studyplan/top-interview-150/
 *
 * Problem:
 * Merge two sorted arrays nums1 and nums2 into nums1 (nums1 has enough space).
 * Do not return anything, modify nums1 in-place.
 *
 * Approach:
 * Use three pointers starting from the end (to avoid overwriting).
 * Compare elements from nums1 and nums2, place larger one at the end of nums1.
 *
 * Time Complexity: O(m + n)
 * Space Complexity: O(1)
 *
 * Learned: Working backwards avoids shifting elements.
 */
class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {

        // Pointer to last real element in nums1
        int p1 = m - 1;

        // Pointer to last element in nums2
        int p2 = n - 1;

        // Pointer to last position in nums1
        int p3 = m + n - 1;

        // Merge until nums2 is exhausted
        while (p2 >= 0) {

            // If nums1 still has elements AND nums1 element is bigger
            if (p1 >= 0 && nums1[p1] > nums2[p2]) {
                nums1[p3] = nums1[p1];
                p1--;
            } 
            // Otherwise take from nums2
            else {
                nums1[p3] = nums2[p2];
                p2--;
            }

            // Move write pointer
            p3--;
        }
    }
}
